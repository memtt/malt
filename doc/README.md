Building the documentation
==========================

In order to build the documentation with sphinx, you need to :

```sh
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
make html
make pdf
```
